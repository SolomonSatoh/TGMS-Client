import React from 'react';
/**
 * Show an error page if a wrong url is entred
 * @returns The ErrorPage component is being returned.
 * Show an error page if a wrong url is entred
 */
function ErrorPage() {
  return <div className='home' > ERROR! PAGE NOT FOUND </div>;
}

export default ErrorPage;

